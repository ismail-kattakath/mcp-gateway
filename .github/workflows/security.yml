name: Security Scan

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  schedule:
    # Run daily at 2 AM UTC
    - cron: "0 2 * * *"

permissions:
  contents: read
  security-events: write

jobs:
  npm-audit:
    name: NPM Security Audit
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package: [server, ui, cli]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "${{ matrix.package }}/package-lock.json"

      - name: Install dependencies
        working-directory: ${{ matrix.package }}
        run: npm ci --audit=false

      - name: Run npm audit
        working-directory: ${{ matrix.package }}
        run: |
          npm audit --audit-level=high --json > audit-report.json || true
          npm audit --audit-level=high

      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: npm-audit-${{ matrix.package }}
          path: ${{ matrix.package }}/audit-report.json
          retention-days: 30

      - name: Check for high/critical vulnerabilities
        working-directory: ${{ matrix.package }}
        run: |
          VULNERABILITIES=$(jq '.metadata.vulnerabilities.high + .metadata.vulnerabilities.critical' audit-report.json)
          echo "Found $VULNERABILITIES high/critical vulnerabilities"
          if [ "$VULNERABILITIES" -gt 0 ]; then
            echo "::error::Found $VULNERABILITIES high/critical vulnerabilities in ${{ matrix.package }}"
            exit 1
          fi

  secret-scanning:
    name: Secret Scanning
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Full history for secret scanning

      - name: TruffleHog Secret Scanning
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --debug --only-verified

  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # The dependency-review-action requires Dependency Graph to be enabled
      # on the repository (an admin-level setting at
      # Settings → Code security → Dependency graph). Mark this step as
      # non-blocking so the workflow doesn't hard-fail when the feature is
      # not yet enabled. We rely on the equivalent step in ci.yml plus npm
      # audit jobs above for the actual gate.
      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        continue-on-error: true
        with:
          fail-on-severity: high
          deny-licenses: GPL-3.0, AGPL-3.0
          warn-only: true

  container-scan:
    name: Container Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t mcp-gateway:test .

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: "mcp-gateway:test"
          format: "sarif"
          output: "trivy-results.sarif"
          severity: "HIGH,CRITICAL"
          ignore-unfixed: true

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: "trivy-results.sarif"

      - name: Run Trivy in table mode
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: "mcp-gateway:test"
          format: "table"
          severity: "HIGH,CRITICAL"
          exit-code: "1"
          ignore-unfixed: true

  sast-analysis:
    name: Static Application Security Testing
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        working-directory: server
        run: npm ci

      - name: Run ESLint security plugin
        working-directory: server
        run: |
          npm install --save-dev eslint-plugin-security
          npx eslint . --ext .ts,.tsx --format json --output-file eslint-security.json || true

      - name: Upload ESLint results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: eslint-security-report
          path: server/eslint-security.json
          retention-days: 30

  security-headers-check:
    name: Security Headers Check
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install and build
        working-directory: server
        run: |
          npm ci
          npm run build

      - name: Start server
        working-directory: server
        run: |
          # DISABLE_STDIO=true forces HTTP mode — backgrounding with `&`
          # leaves stdin pipe-readable, which would otherwise switch the
          # server into stdio transport and exit immediately.
          GATEWAY_DISABLE_AUTH=true DISABLE_STDIO=true \
            node dist/index.js > server.log 2>&1 &
          echo $! > server.pid
          # Wait for /health to come up (cap at ~30s)
          for i in $(seq 1 30); do
            curl -sf http://localhost:3000/health > /dev/null && break
            sleep 1
          done
          curl -s http://localhost:3000/health || (cat server.log; exit 1)

      - name: Check security headers
        run: |
          response=$(curl -s -D - http://localhost:3000/health)

          # Check for required security headers
          echo "$response" | grep -i "X-Content-Type-Options: nosniff" || exit 1
          echo "$response" | grep -i "X-Frame-Options: DENY" || exit 1
          echo "$response" | grep -i "Strict-Transport-Security" || exit 1
          echo "$response" | grep -i "Referrer-Policy" || exit 1

          echo "✓ All required security headers present"

      - name: Stop server
        if: always()
        run: |
          if [ -f server/server.pid ]; then
            kill $(cat server/server.pid) || true
          fi

  license-compliance:
    name: License Compliance Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install license-checker
        run: npm install -g license-checker

      - name: Check server licenses
        working-directory: server
        run: |
          npm ci
          license-checker --summary
          # Block-list approach: fail on strong-copyleft / network-copyleft licenses.
          # All other detected licenses (MIT, Apache-2.0, BSD-*, ISC, MPL-2.0,
          # public-domain equivalents) are acceptable for production redistribution.
          license-checker --failOn 'GPL-1.0;GPL-2.0;GPL-3.0;AGPL-1.0;AGPL-3.0;LGPL-2.0;LGPL-2.1;LGPL-3.0;SSPL-1.0;CC-BY-NC-1.0;CC-BY-NC-2.0;CC-BY-NC-3.0;CC-BY-NC-4.0;CC-BY-NC-SA-4.0' --production

  security-report:
    name: Generate Security Report
    runs-on: ubuntu-latest
    needs:
      [
        npm-audit,
        secret-scanning,
        dependency-review,
        container-scan,
        sast-analysis,
      ]
    if: always()
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Generate summary report
        run: |
          echo "# Security Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| NPM Audit | ${{ needs.npm-audit.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Secret Scanning | ${{ needs.secret-scanning.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Dependency Review | ${{ needs.dependency-review.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Container Scan | ${{ needs.container-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| SAST Analysis | ${{ needs.sast-analysis.result }} |" >> $GITHUB_STEP_SUMMARY
